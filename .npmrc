# Serverless Framework v3 resolves plugins twice under pnpm symlinks and
# fails with "Cannot redefine property: _serverlessExternalPluginName".
# A flat node_modules, like npm produces, avoids it.
node-linker=hoisted
